###############################################
# This app inherits env from the repo root.  #
# Copy `.env.example` from the root to `.env` #
# at the workspace root and run the API.      #
###############################################

# Only override API-specific values here if needed.
# Otherwise, use the root-level `.env`.

# PORT=3000

# Address validation (API preflight)
ADDRESS_VALIDATION_MODE=remote # remote|off (local/both reserved for future validators)
ADDRESS_AUTOFIX=false

# Shipper (origin) defaults
SHIPPER_NAME=
SHIPPER_COMPANY=
SHIPPER_STREET1=
SHIPPER_STREET2=
SHIPPER_CITY=
SHIPPER_STATE=
SHIPPER_ZIP=
SHIPPER_COUNTRY=
SHIPPER_PHONE=
SHIPPER_EMAIL=

# n8n Webhooks
N8N_WEBHOOK_BASE_URL=
N8N_WEBHOOK_INTAKE_PATH=
N8N_WEBHOOK_RATES_PATH=
N8N_WEBHOOK_BOOK_PATH=
N8N_WEBHOOK_TRACKING_PATH=